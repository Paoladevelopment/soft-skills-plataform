export enum LayoutNodeType {
  Objective = 'objectiveNode',
  Task = 'taskNode',
  Title = 'titleNode'
}

export enum RoadmapVisibility {
  Private = 'private',
  Public = 'public',
  Unlisted = 'unlisted'
}
